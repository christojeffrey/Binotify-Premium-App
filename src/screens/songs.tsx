import { FeaturesSongs } from "@features/songs/songs";
import { Box } from "@mui/material";

export const ScreensSongs = () => {
  return (
      <Box >
        <FeaturesSongs />
      </Box>
  )
    
};
